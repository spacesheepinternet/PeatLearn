# PeatLearn Environment Variables
# Copy this file to .env and fill in your API keys

# Google Gemini API Key (required)
GEMINI_API_KEY=your_gemini_api_key_here

# Pinecone API Key (required for vector search)
PINECONE_API_KEY=your_pinecone_api_key_here

# OpenRouter API Key (recommended in prod — enables the Cohere rerank-4-pro
# reranker. Without it, retrieval falls back to a local cross-encoder.)
# OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: Other providers (not used by default)
# OPENAI_API_KEY=your_openai_key_here
# ANTHROPIC_API_KEY=your_anthropic_key_here

# --- Deployment (docker-compose / Caddy) -------------------------------------
# Production domain Caddy serves and auto-provisions HTTPS for.
# Leave unset for local dev (defaults to plain HTTP on :80).
# SITE_ADDRESS=peatlearn.com
# Email Let's Encrypt uses for cert notices (required for prod HTTPS).
# ACME_EMAIL=you@example.com
